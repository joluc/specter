apiVersion: config.joluc.de/v1alpha1
kind: SpecterConfig
metadata:
  labels:
    app.kubernetes.io/name: specter
    app.kubernetes.io/managed-by: kustomize
  name: specterconfig-sample
spec:
  # TODO(user): Add fields here
